{
  "name": "@limitkit/redis",
  "version": "1.2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@limitkit/core": "^1.1.0"
  },
  "peerDependencies": {
    "ioredis": "^5.0.0",
    "redis": "^5.0.0"
  },
  "peerDependenciesMeta": {
    "ioredis": {
      "optional": true
    },
    "redis": {
      "optional": true
    }
  },
  "keywords": [
    "rate-limiter",
    "rate-limiting",
    "redis",
    "token-bucket",
    "leaky-bucket",
    "sliding-window",
    "gcra",
    "nodejs"
  ],
  "description": "Redis store for LimitKit",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/alphatrann/limitkit"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "redis": "^5.11.0"
  }
}
